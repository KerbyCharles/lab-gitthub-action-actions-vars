name: Desafio de Variaveis e Escopos

on:
  workflow_dispatch:

env:
  APP_NAME: "MinhaAPI"

jobs:
  build_app:
    runs-on: ubuntu-latest

    env:
      BUILD_VERSION: "1.0.0"

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ✅ PASSO 1
      - name: Variáveis em Escopos Diferentes
        run: |
          echo "App: $APP_NAME"
          echo "Versão: $BUILD_VERSION"

      # ✅ PASSO 2
      - name: STEP_TEMP dentro do step
        env:
          STEP_TEMP: "temporario"
        run: |
          echo "STEP_TEMP: $STEP_TEMP"

      - name: STEP_TEMP fora do step
        run: |
          echo "STEP_TEMP: $STEP_TEMP"

      # ✅ PASSO 3
      - name: Variáveis padrão
        run: |
          echo "O usuário $GITHUB_ACTOR disparou este workflow no repositório $GITHUB_REPOSITORY. O sistema operacional do runner é $RUNNER_OS."

      # (se quiser manter seus testes, pode deixar)
      #- name: Instalar dependências
        #run: |
         # python -m pip install --upgrade pip
          #pip install pytest

      #- name: Rodar Testes
        #run: pytest test_main.py

      # ✅ PASSO 4
      - name: Gerar hash
        run: |
          RANDOM_NUM=$RANDOM
          echo "BUILD_HASH=$RANDOM" >> $GITHUB_ENV

      # ✅ PASSO 5
      - name: Usar hash
        run: |
          echo "O hash gerado para este build foi: $BUILD_HASH"

  deploy_app:
    runs-on: ubuntu-latest

    steps:
      # ✅ PASSO 6
      - name: Secrets e Vars
        run: |
          echo "URL: ${{ vars.URL_AMBIENTE }}"
          echo "TOKEN: ${{ secrets.API_TOKEN_PROD }}"
